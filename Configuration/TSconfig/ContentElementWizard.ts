mod.wizards.newContentElement.wizardItems.plugins {
	elements{
		upload_example{
			iconIdentifier = ext-upload-icon
			title = Upload Image
			description = Plug and play example extension for multiple or single media upload from frontent
			tt_content_defValues{
				CType = list
				list_type = uploadexample_piexample
			}
		}
	}
}